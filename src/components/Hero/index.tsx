import { Form, StyledDiv, HeroTitle } from './styles'

const Hero = () => (
  <Form>
    <StyledDiv>
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </StyledDiv>
  </Form>
)

export default Hero
